# A data structure that stores multiple pieces of data

# Creating a tuple
coordinates = (12, 54)  # Tuples are immutable i.e. cannot be changed or modified
# coordinates[0] = 20     # Tuples cannot be changed. It will throw an error
print(coordinates[0])  # Accessed in the same way as lists
